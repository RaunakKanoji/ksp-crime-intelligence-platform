{
  "command": "sh -c 'PORT=${X_ZOHO_CATALYST_LISTEN_PORT:-3000} npm run start'",
  "build_path": ".",
  "stack": "node22",
  "env_variables": {},
  "memory": 256,
  "scripts": {
    "predeploy": "npm install && npm run build"
  }
}
